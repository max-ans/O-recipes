const initialState = {
  email: '',
  password: '',
  isLogged: false,
  user: '',
};

const auth = (state = initialState, action = {}) => {
  switch (action.type) {
    default: return state;
  }
};

export default auth;
